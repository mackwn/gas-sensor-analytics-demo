from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Gas sensor analytics and calibration workflow.',
    author='Spencer M.',
    license='',
)
